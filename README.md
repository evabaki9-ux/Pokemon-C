# repo-1

Placeholder repository — scaffolded, stack to be decided later.

## Status

- Git repo initialised at the workspace root (`/home/user`), default branch `main`.
- Initial commit contains only this README and a `.gitignore`.
- No language/framework chosen yet — add source files as the project takes shape.

## Layout

```
/home/user
├── .git/          # version control (note: excluded from workspace snapshots)
├── .gitignore
└── README.md
```

## Next steps

1. Pick a stack (Python, Node/TS, …) and scaffold the project.
2. Rename the repo if `repo-1` is not the final name: edit this file and the folder/repo title.
3. Add a `LICENSE` once the licence is decided.
4. Add a remote and push:

   ```bash
   git remote add origin <your-repo-url>
   git push -u origin main
   ```
